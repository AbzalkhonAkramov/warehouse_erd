// Removed: adding stock quantity now happens inside the product card's history
// modal (Products → click a product → "Add to stock"). Kept as an inert stub.
export default function StockPage() {
  return null;
}
